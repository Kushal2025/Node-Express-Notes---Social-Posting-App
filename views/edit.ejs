<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Edit Post</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-900 text-gray-100 min-h-screen flex items-center justify-center">

  <div class="w-full max-w-lg bg-gray-800 rounded-2xl shadow-lg p-8">
    <h2 class="text-2xl font-semibold text-center mb-6 text-white">
      ✏️ Edit Your Post
    </h2>

    <form action="/update/<%= post._id %>" method="POST" class="space-y-6">
      <div>
        <label for="content" class="block text-sm font-medium text-gray-300 mb-2">
          Post Content
        </label>
        <textarea
          name="content"
          id="content"
          rows="6"
          required
          class="w-full px-4 py-3 text-gray-100 bg-gray-700 border border-gray-600 rounded-xl
                 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500
                 placeholder-gray-400 resize-none"
          placeholder="Update your thoughts..."
        ><%= post.content %></textarea>
      </div>

      <div class="flex justify-between items-center">
        <a href="/profile"
           class="text-gray-400 hover:text-gray-200 transition">
          ← Back to Profile
        </a>
        <button
          type="submit"
          class="px-6 py-2 bg-indigo-600 hover:bg-indigo-700 rounded-xl text-white font-medium
                 transition shadow-md hover:shadow-lg">
          Update Post
        </button>
      </div>
    </form>
  </div>

</body>
</html>
